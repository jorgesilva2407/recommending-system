apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: jorgesilva
  name: project2-pvc-jorgesilva
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: default-storage-class
